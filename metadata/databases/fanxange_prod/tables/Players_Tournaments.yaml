table:
  name: Tournaments
  schema: Players
array_relationships:
  - name: Matches
    using:
      foreign_key_constraint_on:
        column: tournament_id
        table:
          name: Matches
          schema: Players
  - name: Playing_squads
    using:
      foreign_key_constraint_on:
        column: tournament_id
        table:
          name: Playing_squad
          schema: Players
